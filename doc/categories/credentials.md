# Credentials

ProviderCredentials holds the API key and connection settings a client needs.
The environment loader reads keys from a map you supply, such as the process
environment, and builds credentials for one provider or for every provider whose
key is present.
